<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="App.Theme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/toolbar_color</item>
        <item name="colorPrimaryVariant">#ca2c2c</item>
        <item name="colorOnPrimary">#b42727</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/toolbar_color</item>
        <item name="colorSecondaryVariant">#ca2c2c</item>
        <item name="colorOnSecondary">#b42727</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Splash Screen Theme (API >= 31) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- The splash screen background, animated icon, and animation duration -->
        <item name="windowSplashScreenBackground">@color/purple_200</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/witzeal</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <item name="postSplashScreenTheme">@style/App.Theme</item>
    </style>
</resources>